apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kube-devops.fullname" . }}
  labels:
    {{- include "kube-devops.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kube-devops.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "kube-devops.fullname" . }}
    namespace: {{ .Release.Namespace }}
